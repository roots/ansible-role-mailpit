---
mailpit_arch: "{{ 'arm64' if ansible_architecture == 'aarch64' else 'amd64' }}"
mailpit_install_dir: /opt/mailpit
mailpit_version: 1.3.8
mailpit_release_url: "https://github.com/axllent/mailpit/releases/download/v{{ mailpit_version }}/mailpit-linux-{{ mailpit_arch }}.tar.gz"
